There is a need for altering how bug works
other than the PMIT (poor man's issue tracker)
environment variable.

1. DefaultDescriptionFile string `json:"DefaultDescriptionFile"`
2. ImportXmlDump          bool   `json:"ImportXmlDump"`
3. ImportCommentsTogether bool   `json:"ImportCommentsTogether`

