package issues

import (
	//"fmt"
	"testing"
)

func TestFormats(t *testing.T) {
	t.Skip("unused output so far")
	//test: func (b Bug) ToJSONString() (string, error)
	/* config := Config{}
	test := tester{} // from Bug_test.go
	test.Setup()
	defer test.Teardown()
	b := test.bug
	out, _ := ToJSONString()
	expected := "unused output so far"
	if out != expected {
		t.Error(fmt.Sprintf("Failed %s: expected %v but got %v", "Formats ToJSONString", expected, out))
	} */
}
